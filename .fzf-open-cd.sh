# function fzf_open_cd() {
#     local selected_dir=$(bash ~/.dotfiles/fzf-open-cd.bash)
#     if [[ -n "$selected_dir" && -d "$selected_dir" ]]; then
#         cd "$selected_dir"
#     fi
# }

# bindkey -s '^f' 'fzf_open_cd\n'
